#!/bin/bash
secret=$1
key=$2
aws secretsmanager get-secret-value --region us-west-2 --secret-id $secret --query='SecretString' | python3 -c "import sys, json; print(json.loads(json.load(sys.stdin))['$key'])"    
